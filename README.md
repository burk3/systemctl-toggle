# systemctl-toggle

Starts a stopped service. Stops a started service. That's it.

```
systemctl-toggle nftables.service
systemctl-toggle --user wlsunset.service
```

Tab completion works, unlike whatever shell alias you were about to write.
